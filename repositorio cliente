from cliente import Cliente

class RepositorioCliente(Cliente):

    def __init__(self, cpf: str):
        super().__init__(cpf)
        self.clientes = []

    def cadastrar(self, cliente: Cliente):
        for novo in self.clientes:
            if novo.getCpf() == cliente.getCpf():
        
